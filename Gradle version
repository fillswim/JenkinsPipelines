pipeline {
    agent {
        label 'builder'
    }

    tools {
        gradle 8.2
    }

    stages {
        stage('Git pull') {
            steps {
                echo "====== Git pull ======"
                sh 'gradle --version'
            }
        }

    }
}