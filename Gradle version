pipeline {
    agent {
        label 'builder'
    }

    tools {
        gradle 'Gradle 8.1.1'
    }

    stages {
        stage('Git pull') {
            steps {
                echo "====== Git pull ======"
                sh 'gradle --version'
            }
        }

    }
}