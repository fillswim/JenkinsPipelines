pipeline {
    agent {
        label 'builder'
    }

    tools {
        gradle 'Gradle 8.1.1'
    }

    stages {
        stage('Gradle version') {
            steps {
                echo "====== Gradle version ======"
                sh 'gradle --version'
            }
        }

    }
}